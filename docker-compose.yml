services:
  postgres:
    image: postgres:16-alpine
    container_name: estuda-db
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
      POSTGRES_DB: estuda
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
